-- Production-ready auth removes demo users and sample app data.
-- Keep this file intentionally empty so local/linked seed runs do not recreate
-- fake login credentials, seeded profiles, groups, expenses, or notifications.
select 'Sajha Kharcha seed is intentionally empty.' as message;
